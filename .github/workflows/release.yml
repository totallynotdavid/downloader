name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    environment:
      name: publishing

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: "1.3.14"

      - uses: biomejs/setup-biome@10e64aca80761689d9208c85336d4b82ef0d62f7 # v2.7.1
        with:
          version: "2.5.2"

      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - run: npm install -g npm@latest

      # prepublishOnly runs the typecheck, which needs @types/node and
      # @types/bun from node_modules.
      - run: bun install --frozen-lockfile

      # npm publish relies on prepublishOnly to build publish artifacts.
      - run: npm publish

  github:
    needs: publish
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v7

      - uses: softprops/action-gh-release@a4f117a9dd5572319a486154ec1147aebabd285c # v3.0.1
        with:
          generate_release_notes: true
